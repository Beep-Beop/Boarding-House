# Reminders

## Before Final Demo
- [ ] Switch `gui/api_client.py` base_url to `https://BeepBeop-boarding-house-api.hf.space`
- [ ] Verify all HF Space secrets are set (DB, JWT, SMTP, R2, Google OAuth)
- [ ] Test login via admin account
- [ ] Test that Aiven MySQL allows HF Space IP
- [ ] Run `pytest test/ -v` to confirm tests pass
